# Telegram Training Assistant Bot

Персональный помощник для тренировок в Telegram, который помогает планировать тренировки, отслеживать прогресс и получать советы по фитнесу.

## Возможности

- 📋 **Управление профилем** — заполнение данных о возрасте, весе, целях и уровне подготовки
- 💪 **Темплейты тренировок** — выбор из готовых программ тренировок для дома и зала
- 📊 **Отслеживание прогресса** — запись результатов и визуализация в виде графиков
- 🔔 **Напоминания** — настройка уведомлений о тренировках и питьевом режиме
- 🤖 **ИИ-помощник** — ответы на вопросы о тренировках через OpenAI API
- 📈 **Анализ прогресса** — графики динамики веса и выполненных тренировок

## Установка

### Требования

- Python 3.8+
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))
- OpenAI API Key (опционально, для интеллектуальных ответов)

### Шаги установки

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd training_assistent
```

2. Создайте виртуальное окружение:
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate  # Windows
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Заполните `.env` файл своими данными:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
OPENAI_API_KEY=your_openai_key_here  # опционально
```

## Запуск

### Локальный запуск (polling)

```bash
python bot.py
```

### Запуск с webhook (для продакшена)

Установите `WEBHOOK_URL` в `.env` и запустите бота. Убедитесь, что у вас настроен HTTPS сервер для приёма webhook'ов.

## Команды бота

- `/start` — приветствие и описание возможностей
- `/help` — список всех команд
- `/profile` — заполнение или обновление профиля
- `/workout` — выбор темплейта тренировки на сегодня
- `/progress` — просмотр прогресса и графиков
- `/reminders` — управление напоминаниями
- `/setreminder <type> <HH:MM> <daily|weekly>` — установка напоминания
- `/createplan` — создание плана тренировок
- `/logworkout` — ручная отметка выполненной тренировки

## Структура проекта

```
training_assistent/
├── bot.py                 # Точка входа
├── config.py              # Конфигурация (Pydantic)
├── handlers/              # Обработчики команд и сообщений
├── services/              # Бизнес-логика
│   ├── workout_service.py
│   ├── workout_templates.py  # Темплейты тренировок
│   ├── progress_service.py
│   ├── reminder_service.py
│   └── openai_service.py
├── repositories/          # Хранение данных
├── analytics/             # Генерация графиков
├── utils/                 # Утилиты
└── data/                  # JSON файлы с данными
```

## Темплейты тренировок

Бот включает готовые темплейты тренировок для разных уровней подготовки:

- **Домашние тренировки**: ноги, спина, кардио, всё тело
- **Тренировки в зале**: ноги, спина, грудь, кардио
- **Уровни**: начальный (beginner), средний (intermediate)

Темплейты автоматически подстраиваются под ваш профиль (вес, уровень подготовки).

## Хранение данных

Все данные хранятся в JSON файлах в директории `data/`:
- `users.json` — профили пользователей
- `progress.json` — история прогресса
- `reminders.json` — настройки напоминаний
- `workouts/<user_id>.json` — планы тренировок

## Безопасность

⚠️ **Важно**: Бот не является медицинским специалистом. Все ответы, связанные со здоровьем, содержат дисклеймер о необходимости консультации с врачом.

## Разработка

### Запуск тестов

```bash
pytest tests/
```

### Логирование

Логи сохраняются в `logs/bot.log` и выводятся в консоль.

## Лицензия

MIT

## Автор

Создано для помощи в тренировках и отслеживании прогресса.

